---
import "@/global.css";
import "@fontsource-variable/inter";
import { ClientRouter } from "astro:transitions";
import Link from "@/components/ui/Link.astro";

interface Props {
  title: string;
  description?: string;
  image?: string;
  transitions?: boolean;
  transitionFallback?: "animate" | "swap" | "none";
}

const {
  title,
  description,
  image,
  transitions = true,
  transitionFallback = "animate",
} = Astro.props;
const pageTitle = title === "AppHub" ? title : `${title} - AppHub`;
---

<!doctype html>
<html lang="en" class="scheme-light dark:scheme-dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="application-name" content="AppHub" />
    <meta name="color-scheme" content="light dark" />
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)" />
    <meta name="theme-color" content="#09090b" media="(prefers-color-scheme: dark)" />
    <meta property="og:site_name" content="AppHub" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content={image ? "summary_large_image" : "summary"} />
    <meta name="twitter:title" content={pageTitle} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="alternate" type="application/json" href="/catalog.json" title="AppHub catalog" />
    <title>{pageTitle}</title>
    {description && <meta name="description" content={description} />}
    {description && <meta property="og:description" content={description} />}
    {description && <meta name="twitter:description" content={description} />}
    {image && <meta property="og:image" content={image} />}
    {image && <meta name="twitter:image" content={image} />}
    {transitions && <ClientRouter fallback={transitionFallback} />}
  </head>
  <body
    class="m-0 flex min-h-screen flex-col bg-white font-sans text-zinc-950 dark:bg-zinc-950 dark:text-zinc-50">
    <header class="mx-auto grid w-full max-w-6xl grid-cols-[1fr_auto_1fr] items-center p-6">
      <a class="flex items-center gap-2 justify-self-start text-xl font-bold" href="/">
        <img src="/favicon.svg" alt="" width="32" height="32" />
        <span class="hidden sm:inline">AppHub</span>
      </a>
      <nav class="flex gap-3 sm:gap-4" aria-label="Main navigation">
        <Link href="/">Home</Link>
        <Link href="/categories/">Categories</Link>
        <Link href="/trending/">Trending</Link>
      </nav>
      <div aria-hidden="true"></div>
    </header>
    <main class="mx-auto w-full max-w-6xl grow p-6">
      <slot />
    </main>
    <footer class="mt-12 border-t border-zinc-200 dark:border-zinc-800">
      <div class="mx-auto flex max-w-6xl items-center justify-between gap-6 p-6">
        <a class="flex items-center gap-2 font-bold" href="/">
          <img src="/favicon.svg" alt="" width="32" height="32" />
          AppHub
        </a>
        <nav class="flex gap-4" aria-label="Footer navigation">
          <Link href="/privacy/">Privacy</Link>
          <Link href="#">AppHub repository</Link>
          <Link href="https://github.com/kem-a/AppManager">AppManager</Link>
        </nav>
      </div>
    </footer>
  </body>
</html>
